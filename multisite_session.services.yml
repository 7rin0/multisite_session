# Multisite Services.
services:
  # Add subscriber.
  multisite_session.core.subscriber:
    class: Drupal\multisite_session\Event\MultisiteSessionSubscriber
    tags:
      - { name: event_subscriber }
